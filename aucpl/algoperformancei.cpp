
#include <bits/stdc++.h>
using namespace std;

int solve()
{
    int m{};
    cin >> m;
    return m;
    la
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long a{};
    cin >> a;
    long long b{10000001};
    long long c{};
    while (a--)
    {
        cin >> c;
        if (b > c)
        {
            b = c;
        }
    }
    cout << b;
    return 0;
}