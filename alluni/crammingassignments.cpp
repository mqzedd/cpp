#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using vi = vector<int>;
using vll = vector<ll>;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int a{};
    int b;
    vi c;
    set<int> d;
    cin >> a;
    for (int i = 1; i <= a; i++)
    {
        cin >> b;
        c.push_back(b);
    }
    for (int i = 1; i < a; i++)
    {
        f
                cin >
            b;
        d.insert(b);
    }
}
